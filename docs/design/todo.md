# TODO 优化

## 清除记录 抽屉优化

* [x] 样式需更加美观
* [x] 时间选项, 调整布局, 更加高级
* [x] tab键可切换候选项, ctrl+enter触发 复制+锁定操作, 自动推出， 执行粘贴

## 抽屉菜单优化

* [ ] 快捷键 触发 的 `查看全部` 触发抽屉菜单
    * [ ] 非多选, 高亮时, 右方向键触发, 左方向键关闭
        * [ ] 右方向键触发时, 需要将抽屉菜单显示在高亮项的右侧, 并且
            * [ ] 菜单 竖向排列列表形式: `[{序号, 描述, 图标}]`
            * [ ] 可ctrl+序号快速选择对应功能, 然后自动推出抽屉
            * [ ] 可上下方向键在菜单中移动选择, enter|ctrl+enter 键选择功能
            * [ ] 可esc键关闭抽屉
            * [ ] 可拖拽对菜单排序, 然后更新序号
        * [ ] 高亮时, 通过 ctrl+shift+序号, 触发 右键的抽屉子菜单功能

## 搜索优化

* [ ] c-shift-del 触发强制删除, 自动退出搜索状态

## 数据结构优化

* [ ] 优化数据结构, 提高性能
* [x] 增加标记功能, ctrl+U 标记当前高亮项, 标记为 lock 锁型的icon(再次触发取消lock)
    * [x] 标记代表的功能:
        * [x] 无法被 del/backspace, 或者多选时的del/backspace, 或者指定时段清除的形式 直接删除, 其他的ite正常删除
        * [x] 可通过 ctrl+del 强制删除(多选时, 也强制删除)
* [x] 文件类型增加 原始地址项, 若从文件管理复制

## 增加对剪贴板管理器的键盘监听

* [ ] 普通tab页,
    * [x] ctrl+D 触发 收藏, 多选时触发批量收藏, 高亮时 只触发当前item
* [ ] 收藏tab页
    * [ ] 
* [ ] 所有tab页
    * [x] 多选模式下, ctrl+Enter, enter 触发批量合并复制, 自动加入换行(获取系统换行符)
    * [x] 右方向键, 触发 `查看全部` 抽屉菜单
    * [x] 普通字符触发的搜索, 监听backspace, 在删除最后一个字符自动推出搜索
    * [ ] 多选状态下, 若最后一个多选item取消了, 则自动退出多选状态
    * [ ] 鼠标悬浮时 | 按住shift(100ms), 自动悬浮展示预览: 图片, 文字, 候选加入 excel, pdf(先提供大致方案)
    * [x] 所有图标增加悬浮展示功能提示信息
    * [ ] shift+del: 打开 清除抽屉, tab/shift+tab 可切换选项

## 配置页简化

* [x] 移除 头部 赞赏一行的的所有按钮, 清理代码
* [x] 优化整体布局, 提升高级感
* [x] 数据持久化
    * [x] 保存时间增加不限制条件的选项, 默认不限制
* [x] 设置页 增加子tab形式展示不同的模块
    * [x] 基本配置页: `存储位置` `最大历史条数` `保存时间`
    * [x] 快捷键说明页: 列表展示当前的支持的功能(含层描述), 以及绑定的快捷键, 支持修改自定义快捷键(含重置为不绑定按钮)
    * [x] 功能定制页:
        * [x] 上方: ` 展示主页功能`, 可下拉多选, 选项在下面的列表可自行维护
        * [x] 通过列表形式展示所有的功能, 并提供次级弹窗新增/修改/删除, 支持拖拽排序

## 抽屉页面

* [ ] 普通层
    * [ ] `查看菜单` 选项的功能, 把 统计, 百度搜索, 翻译功能去除
    * [ ] 左右键功能实现, 需要效果如下
        * [ ] 右键 抽屉打开 item的快捷菜单抽屉层(应已有逻辑), 在本层快捷键监听并阻止穿透到普通层:
            * [ ] 上下键可切换 子菜单选中, `enter` / `c-cr` 触发 高亮选中的子菜单(`查看菜单` 功能的子集, 但是展示样式不同, 只显示子菜单)的功能
                * [ ] 子菜单暂定为, 必要合理的功能, 不需要包含 收藏, 上锁/解锁, 删除, 强制删除, 后续可增加功能, 注意支持扩展
            * [ ] 左键/Esc 触发返回普通层
    * [ ] 在普通层, `c-s-序号` 快捷键识别:
        * [ ] 触发 子菜单抽屉层的 对应功能
