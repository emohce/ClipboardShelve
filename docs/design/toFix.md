# 待优化

* [x] 丢失了 部分快捷键, 普通的各tab页:
    * [x] 空格 可触发多选的选中和取消选中, 并自动往下移动
    * [x] 普通tab页(基础层), s-del 可触发 `清除` 的抽屉层, 以及在此层:  c-1等数字 可自动切换选项(拦截不让基础层接收此快捷键)
    * [x] 各页面 tab接收, shift-tab 没有触发相反顺序的 移动(选项需要同步选中)

* [x] 普通层, 多选状态:
    * [x] 普通删除后 清理了不上锁的记录, 上锁的记录 要仍然保持 选中状态, 现在会丢失样式, 但是选中的总数统计还是正确的, 两者不匹配了
        * [x] 多选时 普通删除 需要维持 有锁的没被删除的 选中样式 并维持 高亮item不变(若被删则下移, 最后一个则上移)

* [x] 普通层, 搜索状态:
    * [x] c-del, c-backspace 触发普通删除, 然后应该保留查询关键字, 以及搜索状态

* [x] Shift 单按 100ms+ 触发高亮项预览
    * [x] txt、pic 预览已实现; 其他类型后续补充

