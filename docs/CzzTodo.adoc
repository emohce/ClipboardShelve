= 未完成事项 | Todo

== 原有功能优化

* [ ] 优化:
** [ ] 收藏的剪切记录, 单独记录, 防止在普通列表页, 多选删除时, 若是有被收藏的, 则不删除
** [ ] 单条删除时, 弹窗确认默认选择 确认选项, ctrl+enter 或 enter 可直接删除
*** [ ] 在收藏列表删除时, 默认是取消收藏即可, 不删除剪切记录
** [ ] 额外增加一种保留标记(记录列表对应记录开头增加标记), 并增加28小时自动清理功能(可在设置中配置), 若没有标记的剪切记录, 则自动清理; 可通过ctrl+enter触发|移除 保留标记
** [ ] 搜索时 不用优先匹配收藏的剪切记录, 按时间倒序展示, 通过ctrl+enter 可以切换优先匹配收藏的剪切记录
** [ ] 多选触发操作优化, 通过shift+上下键 可快速选择多个, 并可以批量执行操作(del触发删除, ctrl+enter触发保留标记)
** [ ] tab页优化, 可以通过ctrl+tab切换, 并可以快速定位到指定tab(ctrl+1~9,ctrl+0 触发对应tab及最后一个tab)
*** [ ] tab页 名可以编辑, 并
** [ ] 所有操作可ctrl+z撤销