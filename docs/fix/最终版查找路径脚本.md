# 最终版查找路径脚本（绝对无冲突）

## 🎯 推荐：一行版本（最简单，无冲突，不依赖 require）

直接复制执行，绝对不会报错：

```javascript
console.log('监听程序路径:', `${utools.isMacOs() ? utools.getPath('userData') : utools.getPath('home')}${window.exports.sep}_utools_clipboard_manager_storage`.split('_utools_clipboard_manager_storage')[0] + (process.platform === 'win32' ? 'clipboard-event-handler-win32.exe' : process.platform === 'linux' ? 'clipboard-event-handler-linux' : 'clipboard-event-handler-mac'))
```

---

## 🔧 详细版本（使用唯一变量名，不依赖 require）

如果一行版本不够详细，使用这个版本（所有变量名都带唯一前缀，不使用 require）：

```javascript
// 使用唯一变量名，避免任何冲突，不依赖 require
const _p_sep = window.exports.sep  // 从 window.exports 获取路径分隔符
const _p_base = utools.isMacOs() ? utools.getPath('userData') : utools.getPath('home')
const _p_dbPath = `${_p_base}${_p_sep}_utools_clipboard_manager_storage`
const _p_parentDir = _p_dbPath.split('_utools_clipboard_manager_storage')[0]
const _p_platform = process.platform
const _p_fileName = _p_platform === 'win32' ? 'clipboard-event-handler-win32.exe' : 
                    _p_platform === 'linux' ? 'clipboard-event-handler-linux' : 
                    'clipboard-event-handler-mac'
// 使用字符串拼接代替 path.resolve
const _p_listenerPath = _p_parentDir + _p_fileName

console.log('📁 数据库路径:', _p_dbPath)
console.log('📂 监听程序目录:', _p_parentDir)
console.log('🎯 监听程序路径:', _p_listenerPath)
console.log('📄 文件名:', _p_fileName)

// 检查文件是否存在
const { existsSync } = window.exports
console.log('✅ 文件是否存在:', existsSync(_p_listenerPath) ? '是' : '否')
```

---

## 💡 终极解决方案

### 方法 1: 刷新页面（最简单）

1. 按 `Ctrl + R` 或 `F5` 刷新插件窗口
2. 重新执行脚本

### 方法 2: 清除控制台

1. 在控制台右键 → "Clear console" 或按 `Ctrl + L`
2. 重新执行脚本

### 方法 3: 使用一行版本（推荐）

直接使用上面的一行版本，不声明任何变量，绝对不会冲突。

---

## ✅ 快速操作指南

**Windows 用户**：
1. 执行一行版本脚本，获取路径
2. 下载 `clipboard-event-handler-win32.exe`
3. 放到路径显示的目录（通常是 `C:\Users\{你的用户名}\`）

**Linux/macOS 用户**：
1. 执行一行版本脚本，获取路径
2. 下载对应的监听程序文件
3. 放到路径显示的目录
4. 设置执行权限：`chmod +x [文件路径]`

---

## 🎯 最简单的使用方式

**直接复制这一行执行即可**（不依赖 require）：

```javascript
console.log('监听程序路径:', `${utools.isMacOs() ? utools.getPath('userData') : utools.getPath('home')}${window.exports.sep}_utools_clipboard_manager_storage`.split('_utools_clipboard_manager_storage')[0] + (process.platform === 'win32' ? 'clipboard-event-handler-win32.exe' : process.platform === 'linux' ? 'clipboard-event-handler-linux' : 'clipboard-event-handler-mac'))
```

执行后会直接显示监听程序的完整路径，然后：
1. 复制显示的路径
2. 下载对应的监听程序文件
3. 放到路径显示的目录

完成！
