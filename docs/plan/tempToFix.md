# Temp To fix

* [x] 预览窗口逻辑
    * [x] 触发条件: 基于选择的item, 高亮选中时可通过shift按住100ms触发; 鼠标移动时, 可通过悬浮100ms触发; 当触发条件不存在时自动关闭预览且所有过程不改变主窗口布局样式
    * [x] 行为: 封装成一个基于item 的预览调用函数, 根据item类型触发相应的具体实现（悬浮100ms统一在行级处理，普通图片不再使用 img 即时 mouseenter）
        * [x] 文件形式的图片, 预览展示 图片实际预览+文件基础信息, 预览的图片 要求自动缩放以完整展示在窗口的正中间
        * [x] 图片content的形式, 预览时 直接展示 图片预览, 要求和上面一样, 不能遮挡, 自动缩放居中展示, 不要触发主窗口的高度等变更
        * [x] 多行文本时, 预览 自动居中展示