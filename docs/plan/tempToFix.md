# Temp To fix

* [ ] 数据持久化
    * [ ] 保存时间增加不限制条件的选项, 默认不限制

* [ ] 普通层
    * [ ] `查看菜单` 选项的功能, 把 统计, 百度搜索, 翻译功能去除
    * [ ] 左右键功能实现, 需要效果如下
        * [ ] 右键 抽屉打开 item的快捷菜单抽屉层(应已有逻辑), 在本层快捷键监听并阻止穿透到普通层:
            * [ ] 上下键可切换 子菜单选中, `enter` / `c-cr` 触发 高亮选中的子菜单(`查看菜单` 功能的子集, 但是展示样式不同, 只显示子菜单)的功能
                * [ ] 子菜单暂定为, 必要合理的功能, 不需要包含 收藏, 上锁/解锁, 删除, 强制删除, 后续可增加功能, 注意支持扩展
            * [ ] 左键/Esc 触发返回普通层
    * [ ] 在普通层, `c-s-序号` 快捷键识别:
        * [ ] 触发 子菜单抽屉层的 对应功能

* [ ] 设置页 增加子tab形式展示不同的模块
    * [ ] 基本配置页: `存储位置` `最大历史条数` `保存时间`
    * [ ] 快捷键说明页: 列表展示当前的支持的功能(含层描述), 以及绑定的快捷键, 支持修改自定义快捷键(含重置为不绑定按钮)
    * [ ] 功能定制页:
        * [ ] 上方: ` 展示主页功能`, 可下拉多选, 选项在下面的列表可自行维护
        * [ ] 通过列表形式展示所有的功能, 并提供次级弹窗新增/修改/删除, 支持拖拽排序