# TODO 优化

## 清除记录 抽屉优化

* [x] 样式需更加美观
* [x] 时间选项, 调整布局, 更加高级
* [x] tab键可切换候选项, ctrl+enter触发 清除操作, 自动推出

## 抽屉菜单优化

* [x] 快捷键 触发 的 `查看全部` 触发抽屉菜单
    * [x] 非多选, 高亮时, 右方向键触发, 左方向键关闭
        * [x] 右方向键触发时, 需要将抽屉菜单显示在高亮项的右侧, 并且
            * [x] 菜单 竖向排列列表形式: `[{序号, 描述, 图标}]`
            * [x] 可ctrl+序号快速选择对应功能, 然后自动推出抽屉
            * [x] 可上下方向键在菜单中移动选择, enter|ctrl+enter 键选择功能
            * [x] 可esc键关闭抽屉
            * [x] 可拖拽对菜单排序, 然后更新序号
        * [x] 高亮时, 通过 ctrl+shift+序号, 触发 抽屉子菜单功能

## 数据结构优化

* [ ] 优化数据结构, 提高性能
* [x] 增加标记功能, ctrl+L 标记当前高亮项, 标记为 lock 锁型的icon(选择合适的icon), ctrl+shift+L 取消标记
    * [x] 标记代表的功能:
        * [x] 无法被 del/backspace, 或者多选时的del/backspace, 或者指定时段清除的形式 直接删除, 其他的ite正常删除
        * [x] 可通过 ctrl+del 强制删除(多选时, 也强制删除)
* [x] 文件类型增加 原始地址项, 若从文件管理复制

## 增加对剪贴板管理器的键盘监听

* [ ] 普通tab页,
    * [x] ctrl+D 触发 收藏, 多选时触发批量收藏, 高亮时 只触发当前item
* [ ] 收藏tab页
    * [ ] 
* [ ] 所有tab页
    * [x] 多选模式下, ctrl+Enter 触发批量合并复制, 自动加入换行(获取系统换行符)
    * [x] 右方向键, 触发 `查看全部` 抽屉菜单
    * [x] 普通字符触发的搜索, 监听backspace, 在删除最后一个字符自动推出搜索
    * [x] 多选状态下, 若最后一个取消了多选, 则自动退出多选状态
    * [x] 鼠标悬浮时, 自动悬浮展示预览: 图片, 文字, 候选加入 excel, pdf(先提供大致方案)
    * [x] 所有图标增加悬浮展示功能提示信息

## 配置页简化

* [x] 移除 头部 赞赏一行的的所有按钮, 清理代码
* [x] 优化整体布局, 提升高级感