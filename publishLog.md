# 发布说明（用户视角）

**排序**：**最新**一轮永远写在**最上面**（新 `## 日期` 插在紧接本说明之后，旧区块下移）。
**用途**：对外简述用户能感知的变化；实现细节见 [changelog.md](changelog.md)。

## 2026-06-23

* 新增: 外观主题支持亮色、暗色和跟随系统，设置后自有界面与 Element Plus 控件同步切换
* 新增: 多行文本可一键行拼接并粘贴，默认快捷键 `c-s-,`，分隔符可在功能配置中修改
* 新增: 多行文本支持“包围再拼接”和“只包围”，默认快捷键分别为 `c-s-.` 与 `c-s-a-.`
* 优化: 行处理结果只用于本次复制粘贴，不会把临时拼接或包围后的内容重新写入剪贴板历史
* 优化: 包围符支持 `{}`、`[]`、`()`、`<>` 自动配对，`'`、`"`、`` ` `` 等对称符号按一对使用

## 2026-06-17

* 新增: 文件预览大幅增强，支持 PDF、Office、Markdown、JSON/YAML、CSV 等多种格式
* 新增: 文本文件自动识别内容类型并智能渲染，JSON/YAML 显示结构化树状视图
* 新增: PPTX 幻灯片可快速预览文字内容

## 2026-06-14

* 优化: 设置页界面美化，快捷键绑定区 UI 重新设计，更易阅读与配置
* 重构: 快捷键配置体系底层重构——层级优先级统一管理，弹层穿透拦截机制完善，弹窗打开时主界面快捷键不再误响应
* 优化: 快捷键显示格式统一为简短多系统兼容样式（如 `Ctrl+K` / `⌘K`），并与配置存储同步

## 2026-06-10 v1.2.1

* 新增: a-p 置顶/取消置顶，a-g 多选组合编辑/清空
* 新增: a-u/e 单页滚动，c-s-left/right 跳缓存首尾
* 修复: 收藏删除后高亮异常、弹窗 Esc 误退插件、别名清空无效
* 新增: uTools 全局快捷键"粘贴置顶顶部项"和"循环粘贴置顶组合项"

## 2026-06-09 v1.2.0

* 性能: 底层存储全面重构为 SQLite，数据量大时列表加载和搜索速度显著提升。
* 体验: 长列表改用虚拟滚动，条目再多也不会卡顿；滚动更流畅，内存占用更低。
* 搜索: 分页加载 + 索引优化，搜索结果秒出，不再因条目多而变慢。
* 兼容: 首次启动自动迁移旧数据并备份，无需手动操作；保留 JSON 回退机制。
* 新增: 设置页可查看存储运行状态（当前模式、迁移进度等）。

## 2026-04-16

* 新增: 搜索支持别名|分组
* 修复: 搜索时enter, 收藏时c-s的无反应
* 优化: 移动自动滚动优化, 加锁渲染优化

## 2026-04-10

- 小尺寸图片预览时不会被强制放大，显示更自然。
- 图片预览时按 shift+方向键滚动，方向现在正确了。
- 图片预览滚动条样式优化，更好看好用。
- 图片预览界面增加了快捷键提示。
- 多选模式下选中条目进行加锁、别名编辑、收藏等操作时，图标和状态会立即更新，不需要移动条目。
- 弹窗输入时按Esc不会误退插件，快捷键冲突问题修复。
- 增加别名功能(F2触发, 收藏tab触发收藏编辑), 带别名的单个文件可通过(s-cr)以别名粘贴.
- 用户文档中的快捷键说明重新分组，更容易查找；

## 2026-04-08

- 删除剪贴历史后更不容易出现「删掉又自己回来」的现象。
- 多选删除后，光标落在的条目更合理。
- 搜索时更少莫名其妙退出搜索；用中文输入法、还在选字时按 Enter，会先确认到搜索框，而不会误去确认列表里的条目。
- 用键盘上下移动列表时，顶部和底部条目更容易完整看见；选到第一条时不再容易整块滚没。
- 主界面顶栏和列表之间的空白缩小（收藏页带子分类时也更紧凑）。

---
