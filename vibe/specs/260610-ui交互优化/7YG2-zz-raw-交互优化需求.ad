= 交互优化需求


- [ ] 收藏item待修复:
** 收藏tab页, 强制删除后, 会异常跳转到第一项, 正常应该跳转到下一项。
- [ ] 别名编辑交互待修复:
** 别名编辑窗弹出后, 无法直接用esc退出此层, 会直接退出插件, 需要拦截一层
** 收藏item通过F2编辑增加的标签, 取消收藏后, 再进行F2别名编辑时, 编辑别名会生效, 但是清空别名无效

- [ ] 快捷键增加:
** 所有tab, 普通|搜索|多选状态时, 增加 `a-u` `a-e` 的单页上下滚动, 单页即现在正常的条目展示窗口(注意自动滚动和不同类型的高度占位)
** 增加回到已缓存的 顶层/末尾的快捷键 `c-s-leftArrow` 和 `c-s-rightArrow`, 非单页, 非全量, 而是随着上下滚动后自动增加的缓存条目(不要因为不展示就去除缓存)


== 追加需求-1

* [ ] 增加 `a-p` 快捷置顶/取消置顶单个item
** 按当前高亮项执行置顶/取消置顶操作
** 支持同时置顶多个item(都是单个操作), 最新置顶的排在置顶区最上
** 按当前tab/search筛选展示置顶项
** 当前剪贴板临时项无需展示显示在置顶区上方
** 删除item后置顶状态被清理, 置顶只是状态, 没有区域
** 可以通过uTools全局快捷键指令`Ctrl/Cmd+Shift+V`快速粘贴顶顶部项(含当前剪贴板临时项), 若没有置顶项则自动取最顶部的项

== 追加需求-2

* [ ] 增加 `a-g` 快捷置顶/取消 组合多个item,
** 组合置于顶部区域(单行展示, shift可触发预览--类似下方的浮窗展示), 此区域比置顶区高, 组合不影响其内item的原有展示, 可作为独立存储
** 多选时后可触发, 触发时展示浮窗, 列出所有的item, 可以拖动排序, 也可通过 `a-u` `a-e` 移动高亮item下移一个单位, 此时可批量选择进行批量移动(快捷键不变), 不要穿透到下层
** 可以通过uTools全局快捷键指令`c-s-p`, 触发组合内循环粘贴, 自上而下, 自动逐一粘贴, 需要缓存当前应该粘贴哪个项了, 若没有组合则忽略
