# EzClipboard 项目全量需求、存储、交互与迁移总览

> 基线：由 `specs/26040502-vite-tanstack-list/raw/260405-项目全量需求存储交互迁移总览.md` 落盘至本目录；仓库内相对路径按 `cursor/` 已校正；部分锚点已按当前代码对齐（见 §13）。

## 1. 文档目的

- 本文档用于给不同 AI 或工程师提供统一上下文，避免每次从零阅读仓库。
- 文档覆盖 4 个层面：
  - 本项目要解决的真实需求范围
  - 当前数据存储与运行时结构
  - 当前交互与实现方式
  - 已确定的迁移技术方案 S2
- 使用方式：
  - 评估类任务：优先看“项目范围”“现状实现”“迁移方案”
  - 设计类任务：优先看“交互链路”“约束与风险”“迁移边界”
  - 编码类任务：优先看“关键文件”“当前实现方式”“S2 拆分建议”

## 2. 项目定位与核心需求

## 2.1 项目定位

- EzClipboard 是一个基于 `uTools` 的本地剪贴板管理插件，不是普通 Web 站点。
- 运行模式是：
  - `uTools` 主窗口
  - 前端 Vue 页面
  - `preload` 注入宿主能力
  - 本地剪贴板监听程序
  - 本地数据库 / 宿主数据库存储
- 相关基线说明见 [`docs/VersionDesc/20260331-v0-当前版本架构与迭代基线说明.md`](../../../docs/VersionDesc/20260331-v0-当前版本架构与迭代基线说明.md#L1)。

## 2.2 核心业务需求

- 自动监听系统剪贴板变化，识别文本、图片、文件并入库。
- 提供主列表浏览、搜索、筛选、收藏、删除、锁定、复制、粘贴等核心能力。
- 允许收藏数据独立管理，支持标签和备注。
- 支持较强的键盘操作：
  - 单步导航
  - 分页导航
  - 快捷复制/粘贴
  - 多选操作
  - 抽屉、详情、清理弹窗等层级切换
- 作为桌面工具，重点是：
  - 快
  - 稳
  - 键盘友好
  - 长列表不乱

## 2.3 当前重点需求

- 当前正在推进的重点不是“新增一个独立业务模块”，而是：
  - 提升主列表交互性能
  - 修复单步/分页/长按导航问题
  - 修复删除后高亮错位和闪烁
  - 修复锁/收藏状态刷新与行头布局问题
  - 在必要时升级工程底座，使后续优化更可持续

## 3. 当前工程结构

## 3.1 顶层结构

- `src/`
  - Vue 前端页面、组件、hooks、全局运行模块
- `public/`
  - `uTools` 插件入口资源
  - `preload.js`
  - `listener.js`
  - 原生监听程序相关静态文件
- `docs/`
  - 设计说明、历史记录、版本基线、需求文档
- `specs/`
  - 当前任务的 `spec/plan/tasks/verify` 文档

## 3.2 当前入口链路

- 启动入口在 `src/main.js`，会先执行 `initPlugin()`，再创建 Vue 应用。
- 顶层页面由 `App.vue` 组织，主界面核心在 [`src/views/Main.vue`](../../../src/views/Main.vue#L1)。
- 主界面列表核心在 [`src/cpns/ClipItemList.vue`](../../../src/cpns/ClipItemList.vue#L1)。

## 3.3 当前依赖基线

- 目前声明依赖见 [`package.json`](../../../package.json#L1)：
  - `vue`
  - `vue-virtual-scroller`
  - `webpack`
  - `element-plus`
- 当前声明仍偏旧，但锁文件已解析到更新版本，说明工程基线存在漂移风险。

## 4. 运行时与宿主桥接

## 4.1 preload 注入

- [`public/preload.js`](../../../public/preload.js#L1) 把以下能力注入 `window.exports`：
  - `utools`
  - 文件系统能力
  - `listener`
  - Electron `clipboard` / `nativeImage`
  - `time`
  - `Buffer`

## 4.2 前端依赖模式

- 当前前端大量依赖全局对象：
  - `window.exports`
  - `window.db`
  - `window.listener`
  - `window.remove`
- 这意味着项目不是严格依赖注入式架构，而是宿主桥接型架构。

## 4.3 运行时约束

- 任何现代化重构都必须继续兼容：
  - `uTools` 主窗口静态入口
  - `preload.js`
  - 插件环境中的全局能力注入
- 不能把项目误当成纯浏览器 SPA 去重构。

## 5. 数据存储结构

## 5.1 当前主存储

- 当前基线文档说明项目历史上主要使用本地 JSON 文件存储，见 [`docs/VersionDesc/20260331-v0-当前版本架构与迭代基线说明.md`](../../../docs/VersionDesc/20260331-v0-当前版本架构与迭代基线说明.md#L114)。
- 当前 `initPlugin.js` 中仍定义了本地 DB 结构与初始化逻辑，见 [`src/global/initPlugin.js`](../../../src/global/initPlugin.js#L120)。

## 5.2 数据结构

- 当前数据库主结构：
  - `data`: 普通历史记录
  - `collects`: 收藏 ID 列表
  - `collectData`: 收藏完整数据
  - `tags`: 全局标签
  - `tagUsage`: 标签使用统计
  - `createTime`
  - `updateTime`

## 5.3 收藏模型

- 收藏不是简单 `collect: true` 标记，而是“普通历史”和“收藏数据”分离存储。
- 收藏逻辑要点：
  - 收藏时从普通历史迁出，插入收藏集
  - 取消收藏时从收藏集移回普通历史
  - 收藏项可携带 `collectTime`、`tags`、`remark`
- 这会影响：
  - 删除逻辑
  - 搜索逻辑
  - 列表混排逻辑
  - 高亮恢复逻辑

## 5.4 迁移与兼容

- [`src/global/initPlugin.js`](../../../src/global/initPlugin.js#L143) 内有多段兼容历史数据的规范化逻辑，包括：
  - `collect` 旧字段迁移
  - `collectData` 修复
  - `collectTime` 补齐
  - `tags` / `remark` 补齐
  - `locked` 补齐
  - `originPaths` 补齐
- 说明：
  - 当前项目有真实历史包袱
  - 存储层改动不能想当然清空旧兼容逻辑

## 5.5 备用 / 新方向存储

- 仓库中已有 [`src/global/utoolsDB.js`](../../../src/global/utoolsDB.js#L1)，说明项目已经尝试或规划使用 `uTools DB API` 替代 JSON 文件。
- 该实现特征：
  - 以文档 `_id` 做命名空间管理
  - 把历史项、收藏、标签拆成多个文档存储
  - 支持批量写入和清空迁移
- 这意味着后续 AI 评估时需要考虑：
  - 是否继续沿用 JSON 文件
  - 是否切换到 `uTools DB`
  - 是否保持双轨兼容

## 6. 剪贴板监听与入库链路

## 6.1 监听程序

- [`public/listener.js`](../../../public/listener.js#L1) 会按平台启动原生监听程序：
  - Windows `.exe`
  - Linux 可执行文件
  - macOS 可执行文件
- 监听到 `CLIPBOARD_CHANGE` 后，会向前端宿主发事件。

## 6.2 入库流程

- 入口在 [`src/global/initPlugin.js`](../../../src/global/initPlugin.js#L1)。
- 基本链路是：
  1. 初始化宿主能力与数据库
  2. 启动监听
  3. 读取剪贴板
  4. 识别文本 / 图片 / 文件
  5. 转成统一 item
  6. 写入数据库
  7. 推动前端 UI 刷新

## 6.3 相关风险

- 监听失败时有回退逻辑。
- 因为入库链路与 UI 刷新耦合较近，如果重构 UI 层，要避免拿页面层去补监听层问题。

## 7. 当前交互结构

## 7.1 主界面 Main.vue

- [`src/views/Main.vue`](../../../src/views/Main.vue#L1) 当前承担：
  - 主 tab 切换
  - 收藏子 tab
  - 搜索与筛选
  - 展示列表组装
  - 列表删除后的索引恢复
  - 弹窗、详情、标签编辑、清理记录等入口
  - 主界面快捷键 feature 注册

## 7.2 展示列表真相源

- 当前最终展示列表由 [`src/views/Main.vue#L922`](../../../src/views/Main.vue#L922) 到 [`src/views/Main.vue#L931`](../../../src/views/Main.vue#L931) 计算：
  - `showList`
  - `collectBlockList`
  - `currentShowList`
- 这很关键：
  - 所有导航、删除恢复、滚动定位，最终都应该以 `currentShowList` 为准。

## 7.3 列表项交互层

- [`src/cpns/ClipItemList.vue`](../../../src/cpns/ClipItemList.vue#L1) 是当前最复杂的组件。
- 当前同时承担：
  - 虚拟滚动
  - 单步导航
  - 分页导航
  - 长按自动滚动
  - 删除恢复
  - 多选状态
  - 懒加载触发
  - hover / keyboard 预览
  - 行操作快捷键

## 7.4 快捷键体系

- 注册中心在 [`src/global/hotkeyRegistry.js`](../../../src/global/hotkeyRegistry.js#L1)。
- 绑定表在 [`src/global/hotkeyBindings.js`](../../../src/global/hotkeyBindings.js#L1)。
- 特点：
  - 按 layer + state + shortcut 绑定 feature
  - 主界面、设置页、抽屉、弹窗都用同一注册表
  - 对重复按键、Mac 上 ctrl/meta 映射都有兼容逻辑

## 8. 当前列表实现方式

## 8.1 虚拟列表方案

- 当前主列表使用 `vue-virtual-scroller` 的 `DynamicScroller` / `DynamicScrollerItem`。
- 目标是支持长列表和动态高度。

## 8.2 当前实现热点

- “完全可见”判断：
  - [`src/cpns/ClipItemList.vue#L1518`](../../../src/cpns/ClipItemList.vue#L1518)
- 滚动到活动项：
  - [`src/cpns/ClipItemList.vue#L1528`](../../../src/cpns/ClipItemList.vue#L1528)
- 设置高亮索引：
  - [`src/cpns/ClipItemList.vue#L1633`](../../../src/cpns/ClipItemList.vue#L1633)
- 分页步长：
  - [`src/cpns/ClipItemList.vue#L1689`](../../../src/cpns/ClipItemList.vue#L1689)
- 长按自动滚动：
  - [`src/cpns/ClipItemList.vue#L1783`](../../../src/cpns/ClipItemList.vue#L1783)
- 删除恢复 watcher：
  - [`src/cpns/ClipItemList.vue#L1870`](../../../src/cpns/ClipItemList.vue#L1870)
- 主导航 feature：
  - [`src/cpns/ClipItemList.vue#L1931`](../../../src/cpns/ClipItemList.vue#L1931)
- 第二套自动滚动：
  - [`src/cpns/ClipItemList.vue#L2330`](../../../src/cpns/ClipItemList.vue#L2330)

## 8.3 当前主要问题

- 单步滚动与“完全可见则不滚”不一致。
- 长按与分页存在多套逻辑。
- 删除恢复父子双写。
- 虚拟滚动、预览、多选、懒加载耦合过深。
- 行头状态刷新与布局压缩仍依赖主列表刷新时机。

## 8.4 行渲染方式

- 行头结构见 [`src/cpns/ClipItemRow.vue#L19`](../../../src/cpns/ClipItemRow.vue#L19)：
  - 收藏图标
  - 锁图标
  - 时间
  - 标签
- 行头样式见 [`src/style/cpns/clip-item-list.less#L192`](../../../src/style/cpns/clip-item-list.less#L192)。

## 9. 当前需求重点与评估焦点

## 9.1 功能性需求

- 单步导航：完全可见则不滚。
- 长按导航：统一半分页/分页响应。
- 分页导航：与长按行为一致。
- 删除恢复：高亮稳定，不闪烁、不选错。
- 锁/收藏状态：及时刷新。
- 混排搜索 / 收藏块 / 锁筛选：继续可用。

## 9.2 性能需求

- `300ms` 定义为：
  - 单次交互后 UI 达到滚动稳定且高亮正确的完成时间。
- 核心不是跑分，而是：
  - 降低无意义滚动
  - 降低重渲染范围
  - 降低状态竞争

## 9.3 评估时必须关注的边界

- `uTools` 插件运行环境
- `preload.js` 兼容
- 收藏数据分离模型
- 多选、预览、懒加载与键盘体系耦合
- 插件环境与浏览器开发环境差异

## 10. 已确定迁移方案：S2

## 10.1 S2 定义

- 工程底座：
  - `Vue 3.5 + Vite`
- 列表方案：
  - `TanStack Virtual`
- 重构范围：
  - 主列表模块重写
  - 导航状态层重写
  - 滚动策略层重写
  - 保持 `uTools` 插件运行方式

## 10.2 选择 S2 的原因

- 当前问题根因主要在列表交互层，不是单纯旧框架慢。
- 如果只升级构建器，不重写列表内核，收益有限。
- `TanStack Virtual` 更适合高定制键盘交互和 headless 控制。
- `Vite` 更适合作为新的工程基线，能降低后续维护成本。

## 10.3 S2 目标结构

- 构建层：
  - 用 `Vite` 取代 `Vue CLI + webpack 4`
- 列表层：
  - 导航状态层
  - 滚动策略层
  - 行渲染层
- 页面层：
  - `Main.vue` 继续负责 `currentShowList` 真相源和页面级状态编排

## 10.4 S2 不改变的内容

- 不改变 `uTools` 插件形态。
- 不改变数据库业务语义。
- 不改变收藏独立存储模型。
- 不改变主业务能力和主要快捷键语义。

## 11. 对 AI 评估者的建议问题

- 如果按 S2 重构：
  - 是否建议继续保留现有 `Main.vue` 作为页面级状态中心？
  - 是否建议把导航状态提到独立 hook？
  - 是否建议先迁移构建，再重写列表，还是并行推进？
  - 是否建议同时切换到 `uTools DB`？
  - TanStack Virtual 的 item measurement、scrollToIndex、overscan 策略应该如何设计？
- 如果不按 S2：
  - 是否有比 `Vue 3.5 + Vite + TanStack Virtual` 更适合 `uTools` 插件的现代组合？
  - 如果有，切换收益是否显著超过迁移成本？

## 12. 建议结论

- 对不同 AI 的评估任务，建议至少输出以下内容：
  - 是否认同 S2 是当前最优主线
  - 若不认同，替代方案是什么
  - 对本项目的存储层是否需要同步重构
  - 对 `Main.vue` / `ClipItemList.vue` / `hotkeyRegistry` 的重构边界建议
  - 风险最高的 3 个点和对应缓解策略

## 13. 文档评估（cursor 附）

### 13.1 总体结论

- 本文档适合作为**跨工程师 / 跨 AI 会话的统一上下文**：从需求、存储、交互到已选 **S2** 迁移方案，链条完整，与同目录 [`260405-最新框架适配评估.md`](260405-%E6%9C%80%E6%96%B0%E6%A1%86%E6%9E%B6%E9%80%82%E9%85%8D%E8%AF%84%E4%BC%B0.md) 中 **S2（Vue 3.5 + Vite + TanStack Virtual + 列表重写）** 的结论一致。
- **第 10～12 节**把「改什么 / 不改什么」和评估产出物要求写清楚，可直接作为后续 `01-spec` / `02-plan` 的上位输入。

### 13.2 优点

- **宿主约束**（`preload`、`window.*` 桥接、非纯 SPA）表述准确，能避免迁移时误用 SSR 或浏览器中心化假设。
- **`currentShowList` 真相源**（§7.2）与收藏分离模型（§5.3）是列表/高亮/搜索问题的关键上下文，对 S2 拆分（页面编排 vs 列表内核）有直接帮助。
- **§8.2 热点行号**便于快速切入 `ClipItemList.vue`；**§8.3 问题清单**与性能口径（§9.2，`300ms`）使验收可讨论、可验证。
- **§11 建议问题**覆盖构建顺序、hook 边界、`uTools DB` 是否并行、TanStack 策略，利于拆解任务而非一次性臆测实现。

### 13.3 与仓库抽样核对（2026-04-05）

- [`src/views/Main.vue`](../../../src/views/Main.vue) 中 `displayList` / `currentShowList`（约第 922～931 行）与 §7.2 描述一致。
- [`src/cpns/ClipItemList.vue`](../../../src/cpns/ClipItemList.vue) 中 §8.2 所列行号（1518、1528、1633、1689、1783、1870、1931、2330）与当前文件内容匹配。
- **raw 版问题（本 cursor 版已修）**：指向基线文档的部分链接缺少 `docs/` 路径段；§5.1 中「JSON 文件」在基线文档中更贴近约 **L114** 附近表述，故将原 `#L88` 调整为 `#L114`。
- **`initPlugin.js` 行号漂移**：原 §5.1「DB 结构」锚点 `#L96`、§5.4「兼容迁移」锚点 `#L116` 与当前文件不完全一致，本版分别改为 **`#L120`（`defaultDB`）** 与 **`#L143`（首段迁移逻辑）**。

### 13.4 缺口与可选补强

- **插件入口契约**：可补充与 `public/plugin.json`、`development.main`、构建产物目录的对照（与同 spec 下框架评估文档交叉引用即可，避免重复长篇）。
- **分阶段验收**：若 S2 拆成「先 Vite、后 TanStack 列表」，建议在 plan 中单列两段的**独立验收标准**，降低单次变更半径。
- **测试与插件环境**：可点明列表与快捷键的验证以 **uTools 插件环境**为准，`pnpm run serve` 仅覆盖部分行为（与 `AGENTS.md` 验证规则一致即可）。

### 13.5 从文档派生的风险与缓解

| 风险 | 缓解思路 |
|------|----------|
| JSON 与 `utoolsDB` 双轨并存，评估时容易默认「顺便迁库」 | §5.5 已提示取舍；S2 默认**不扩存储大改**，除非单独开 spec 与迁移脚本 |
| `Main.vue` ↔ `ClipItemList` 状态与删除恢复「双写」 | §8.3 已点名；S2 实施时约定**单一导航事实源**或明确父子契约 |
| 重装列表后 `hotkeyRegistry` / feature 名与 layer 不一致 | §7.4、§11；列表适配层或**冻结 feature 注册接口**，先对齐再换实现 |

### 13.6 对 §11 / §12 问题的简要立场（供后续 plan 引用）

- **是否认同 S2 为当前最优主线**：**认同**，与框架适配评估及问题根因（列表交互层而非仅构建器老化）一致。
- **存储层是否同步重构**：**默认不同步**；仅当性能或可靠性证明 JSON 路径是瓶颈时再单独立项。
- **Main 是否继续作页面级状态中心**：**倾向保留**，与 §10.3 一致；导航与滚动细节下沉到 hook / 列表子系统。
- **先 Vite 还是先列表**：**优先控风险则先 Vite + 行为回归**；若资源允许，列表重写可与构建迁移分分支并行，但合并前需统一验收矩阵。

