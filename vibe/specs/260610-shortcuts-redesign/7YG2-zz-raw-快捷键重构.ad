= 快捷键重构需求


- [ ] 快捷键体系重构:
** 参照vscode的快捷键体系, 重新设计和实现。
*** 快捷键主体抽取为 command, 带有id, 可以进行快捷键绑定, 可以有备注, when条件, 有source字段(当前的系统快捷键为默认system, 用户修改即为user, 可回滚默认)
*** when条件改造, 当前所有的默认层, 冲突的优先级, 都统一进行改做升级, 改为触发到when条件, 例如 搜索状态, 普通状态, 预览状态, 多选状态, 浮窗, 侧栏等, 要进行全量的核验逻辑和冲突处理
*** 快捷键变更绑定的冲突判断提醒, 要符合上面的逻辑, 有些快捷键可以在不同的层同时存在, 有些需要判断是否冲突, 有些是状态组合下生效, 有些时状态取反组合, 要支持悬浮when场景选择并可选择: 是|非前置, 以及状态组合与|或|异或等判断
*** 当前的系统command暂时固定(按现有逻辑进行完整command复现, 有些逻辑相似的就融合一个command, 例如删除,强制删除, 要一定的抽象概念或者统一方法, 方法内自动根据快捷键的when去判断是否符合条件触发快捷键, 然后触发到功能要判断是否符合默认限制),
*** 初步固定的command按当前的快捷键进行默认绑定(可以设置不同的when组合, 如删除-批量|普通单个|收藏单个), 允许用户自行绑定, 以及恢复默认, 所有操作都要进行冲突判断
*** 保留后续组合型command的埋点, 比如一个快捷键 command-a, 延迟多少秒后, 执行command-b, 或者同时进行等各类操作组合, 甚至就行层级/状态穿透操作
** 设置->快捷键 页面重构
*** 按vscode形式展示, command位置悬浮时, 可展示备注说明
*** 绑定关系, command的说明, 备注等各字段分两种, 一种是系统初始默认的, 放到本插件默认缓存, 一种是客户的, 当系统版本更新初次加载后, 就把默认的数据放到sqllite, 后续变更都以sqllite信息为准, 即使回滚默认也是回滚sqllite不要影响系统级默认缓存
*** 页面加载也是复用普通tab的展示, 缓存(自增量, 查询过滤都通过sqllite语句), 表头要固定, command行可进行编辑配置,
*** when条件配置:
**** 可直接左键点击文本更改, enter触发提交, 提交需要校验, 若不合格则弹窗展示编辑项, 以及所有的when条件, 以及冲突的快捷键(可直接统一管理编辑)
**** 也可右键弹窗, 展示本编辑项, 以及展示所有的when条件, 有些条件是互斥的